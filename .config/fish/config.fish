alias time "time --portability"
alias s "git status"
alias d "git diff"
alias dc "git diff --cached"
alias dt "git difftool"
alias ff "find . -not \( -path './build*/*' -prune \) -not \( -path './.*/*' -prune \) -not \( -path '*/venv/*' -prune \) -not \( -name '*.pyc' -prune \) -type f"

