alias time "time --portability"
alias s "git status"
alias d "git diff"
alias ff "find . -not \( -path './build*/*' -prune \) -not \( -path './.*/*' -prune \) -not \( -path '*/venv/*' -prune \) -type f"
